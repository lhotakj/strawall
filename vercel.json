{
  "version": 2,
  "regions": [
    "fra1"
  ],
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
          "buildCommand": "pip install --upgrade pip && cd api && pip install -r requirements.txt  && dpkg -i ./install/wkhtmltox_0.12.6.1-2.jammy_amd64.deb && pip install -r requirements.txt",
          "debug": true
      }
    }
  ],
  "routes": [
    {
      "src": "/",
      "dest": "/app.py"
    },
    {
      "src": "/stats.png",
      "dest": "/app.py"
    }
  ],
  "env": {
    "environment": "VERCEL",
    "redis": "localhost"
  }
}
